<h1>Create a new investor</h1>

<fieldset>
	<%= form_for @investor do |f| %>
		<%= render 'shared/error_messages', object: f.object %>
		<%= render 'shared/investor_form', f: f %>
	<% end %>
</fieldset>